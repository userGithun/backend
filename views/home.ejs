
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>mark 1</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f4f4f4;
            
            overflow: hidden;
        }
        .calendar {
            width: 350px;
            background: #ffffff73;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            padding: 20px;
            text-align: center;
            transition: all 0.5s;
            position: relative;
        }
        .grid {
            display: grid;
            grid-template-columns: repeat(7, 1fr);
            gap: 5px;
            margin-top: 10px;
            transition: all 0.5s;
        }
        .day, .date {
            padding: 10px;
            background: #eee;
            border-radius: 5px;
            cursor: pointer;
            transition: all 0.3s;
        }
        .special {
            background: #ff4b5c;
            color: white;
        }
        .special:hover {
            transform: scale(1.1);
        }
        .pin {
            position: absolute;
            top: -10px;
            right: -10px;
            font-size: 20px;
        }
        @keyframes zoomEffect {
            0% { transform: scale(1); opacity: 1; }
            50% { transform: scale(2) translateY(-50px); }
            100% { transform: scale(5) translateY(-100vh); opacity: 0; }
        }
    </style>
</head>
<body style="background: url(image/for\ calen.jpg); background-position: center; background-size: cover; background-repeat: no-repeat;">
    <div class="calendar" id="calendar">
        <h2 style="color: #ff4b5c;">March 2025</h2>
        <div class="grid" id="grid">
            <div class="day">Sun</div>
            <div class="day">Mon</div>
            <div class="day">Tue</div>
            <div class="day">Wed</div>
            <div class="day">Thu</div>
            <div class="day">Fri</div>
            <div class="day">Sat</div>
            
            <script>
                for (let i = 1; i <= 30; i++) {
                    document.write(`<div class='date' onclick='randomPopup()'>${i}</div>`);
                }
            </script>
            <div class="date special" id="specialDate" onclick="zoomToPage()">31<span class="pin">📌</span></div>
        </div>
    </div>
    
    <script>
        function zoomToPage() {
            let specialDate = document.getElementById('specialDate');
            let calendar = document.getElementById('calendar');
            
            // Zoom effect towards the second page
            specialDate.style.animation = "zoomEffect 2s forwards";
            calendar.style.transition = "transform 1s ease-in-out";
            calendar.style.transform = "scale(4) translateY(-50px)";
            
            setTimeout(() => {
                window.location.href = "/about";
            }, 1500);
        }
        function randomPopup() {
            alert("Something special is coming soon! Stay tuned! 🎉");
        }
    </script>
</body>
</html>
